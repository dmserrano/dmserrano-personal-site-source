/*
    Button Components
*/
.btn {
    @apply font-bold py-2 px-4 rounded;
}

.btn-outline {
    @apply font-semibold py-2 px-4 rounded border;

    &:hover {
        @apply border-transparent;
    }
}

.btn-orange {
    @apply bg-orange text-white;

    &:hover {
        @apply bg-orange-dark;
    }
}

.btn-orange-outline {
    @apply border-orange-custom text-orange-custom;

    &:hover {
        @apply bg-orange-custom text-white;
    }
}